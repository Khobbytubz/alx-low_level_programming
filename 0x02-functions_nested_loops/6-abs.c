#include "main.h2"

/**
 * _abs - Check main
 * @r: An integer input
 * Description: computes the absolute value of an integer
 * Return: absolute value of r.
 */
int _abs(int)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
